name: backend
description: Backend server for Modulos system
version: 1.0.0

environment:
  sdk: ^3.13.3

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.4
  postgres: ^3.0.0

dev_dependencies:
  lints: ^2.0.0
